---
consul_services:
  - name: "keycloak-master"
    url : https://keycloak.opstree.dev/
    port: 8080
    token: "{{ consul_agent_token }}"
    group: "role_keycloak"

  - name: "clickhouse"
    url : https://clickhouse.opstree.dev/
    port: 8123
    token: "{{ consul_agent_token }}"
    group: "role_clickhouse"

  # - name: "opensearch"
  #   port: 5601
  #   token: "{{ consul_agent_token }}"
  #   group: "role_opensearch"

  - name: "wazuh"
    url : https://wazuh.opstree.dev/
    port: 5601
    token: "{{ consul_agent_token }}"
    group: "role_wazuh"

  - name: "minio"
    url : https://minio.ldc.opstree.dev:8443/
    token: "{{ consul_agent_token }}"
    group: "role_minio"

  
