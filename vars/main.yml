---
consul_services:
  - name: "keycloak"
    url : https://keycloak.opstree.dev/
    token: "{{ consul_agent_token }}"
    group: "role_keycloak"

  - name: "clickhouse"
    url : https://clickhouse.opstree.dev/
    token: "{{ consul_agent_token }}"
    group: "role_clickhouse"

  # - name: "opensearch"
  #   port: 5601
  #   token: "{{ consul_agent_token }}"
  #   group: "role_opensearch"

  - name: "wazuh"
    port: 5601
    token: "{{ consul_agent_token }}"
    group: "role_wazuh"

  - name: "minio"
    url : https://minio.ldc.opstree.dev:8443/
    token: "{{ consul_agent_token }}"
    group: "role_minio"

  
