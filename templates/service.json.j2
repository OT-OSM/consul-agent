{
  "service": {
    "name": "{{ item.name }}",
    {% if item.port is defined %}"port": {{ item.port }},{% endif %}
    "token": "{{ item.token }}"{% if item.url is defined or item.port is defined %},
    "check": {
      "id": "{{ item.name }}-check",
      "name": "{{ item.name }} Health Check",
      {% if item.url is defined %}
      "http": "{{ item.url }}",
      "tls_skip_verify": true,
      {% else %}
      "http": "http://{{ ansible_default_ipv4.address | default('localhost') }}:{{ item.port }}/",
      {% endif %}
      "interval": "10s",
      "timeout": "2s"
    }{% endif %}
  }
}
